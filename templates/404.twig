{% extends 'base.twig' %}
{% block content %}
  <section class="hero is-danger">
    <div class="hero-body">
      <div class="container has-text-centered">
        <h1 class="title">
          This path does not exist :(
        </h1>
        <a class="button is-info" href="{{site.link}}">
          Go back
        </a>
      </div>
    </div>
  </section>
{% endblock %}
